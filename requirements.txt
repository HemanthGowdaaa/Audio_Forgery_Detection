# ===================================================================
# Root requirements.txt — LOCAL DEVELOPMENT / TRAINING on macOS
# ===================================================================
# This is for running model training locally on macOS (MPS / CPU).
# For Render deployment, see: django_backend/requirements.txt
# ===================================================================

# Core ML
torch>=2.2.0
torchaudio>=2.2.0
scikit-learn>=1.4.0
librosa>=0.10.1
soundfile>=0.12.1
numpy>=1.26.0
scipy>=1.11.0
joblib>=1.3.0
pyyaml>=6.0.1

# HuggingFace
huggingface_hub>=0.23.0

# FastAPI (legacy backend, local testing only)
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.9

# Django (for local Django dev server)
Django>=4.2
django-cors-headers>=4.0
djangorestframework>=3.14
gunicorn>=21.2.0
whitenoise>=6.7.0

# Plotting / analysis
matplotlib>=3.7.0
seaborn>=0.13.0
plotly>=5.15.0
pandas>=2.0.0
